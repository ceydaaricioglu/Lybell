{
  "name": "cursor-deneme",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev:app": "npm run dev -w app",
    "dev:web": "npm run dev -w web",
    "build:app": "npm run build -w app",
    "build:web": "npm run build -w web",
    "android": "npm run android -w app",
    "ios": "npm run ios -w app"
  }
}
